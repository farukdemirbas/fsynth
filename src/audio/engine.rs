// owns cpal stream

pub struct AudioEngine {

}

impl AudioEngine {
    pub fn new() -> Self {
        // prepare audio device/config/stream
    }

    pub fn start(&self) {
        // start stream
    }
}